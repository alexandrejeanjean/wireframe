import React from 'react'
import styled from '@emotion/styled'

const SubmitButton = () => {
  return <SubmitButtonStyled></SubmitButtonStyled>
}

const SubmitButtonStyled = styled('button')``

export default SubmitButton
